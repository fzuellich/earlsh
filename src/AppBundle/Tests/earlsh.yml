shortener:
    # Prevent someone from creating a short url pointing to this earlsh installation.
    prevent_local_urls: true
    hostname: 'localhost:8000'

    # Add some regexes to prevent matching urls from being shortened.
    rejected_sites:
        - http.?://.*porn.